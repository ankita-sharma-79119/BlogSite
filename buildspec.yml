version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
    - appspec.yml
    - requirements.txt
  base-directory: 'blogging'